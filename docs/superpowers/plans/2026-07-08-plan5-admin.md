# Plan 5 — Admin

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ampliar el panel admin (hoy solo stats + reports) con gestión de usuarios (ban/suspender/activar), listado de usuarios, cancelación forzada de planes y moderación de reviews.

**Prerequisito:** Plan 1 (define `UserStatus` enum `active`/`suspended`/`deleted`). Si Plan 1 no corrió, primero añadir `UserStatus` a `models/enums.py` + columna `status` a `User` + migración.

**Architecture:** Endpoints bajo `/admin/*` protegidos por `require_admin`. Las acciones de moderación mutan `User.status` (soft) y disparan revocación de tokens vía `TokenStore.revoke_user`. `require_admin` pasa a lanzar `ForbiddenError` (403) en lugar de `AuthError` (401).

**Tech Stack:** FastAPI, SQLAlchemy async, pytest.

---

## File Structure

- **Modify:** `backend/src/gad/exceptions.py` — añadir `ForbiddenError`.
- **Modify:** `backend/src/gad/admin/dependencies.py` — usar `ForbiddenError`.
- **Modify:** `backend/src/gad/admin/service.py` — funciones de moderación.
- **Modify:** `backend/src/gad/admin/schemas.py` — nuevos schemas.
- **Modify:** `backend/src/gad/admin/router.py` — nuevos endpoints.
- **Modify:** `backend/src/gad/users/service.py` — `set_user_status`.
- **Create:** `backend/tests/test_admin_moderation.py`

---

## Task 1: ForbiddenError + require_admin 403

**Files:**
- Modify: `backend/src/gad/exceptions.py`
- Modify: `backend/src/gad/admin/dependencies.py`

- [ ] **Step 1: Excepción**

En `backend/src/gad/exceptions.py`, añadir:

```python
class ForbiddenError(GADError):
    status_code = 403
    code = "forbidden"
```

- [ ] **Step 2: require_admin usa 403**

En `backend/src/gad/admin/dependencies.py`, cambiar:

```python
from gad.exceptions import ForbiddenError


async def require_admin(
    current_user: Annotated[User, Depends(get_current_user)],
) -> User:
    if not current_user.is_admin:
        raise ForbiddenError("Se requieren permisos de administrador")
    return current_user
```

- [ ] **Step 3: Test + commit**

`backend/tests/test_admin_moderation.py`:

```python
import pytest
from fastapi import FastAPI
from httpx import ASGITransport, AsyncClient
from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker

from gad.admin.router import router as admin_router
from gad.auth.router import router as auth_router
from gad.auth.service import register
from gad.exceptions import GADError
from gad.schemas.auth import RegisterIn


@pytest.fixture
def app(db_engine):
    app = FastAPI()

    @app.exception_handler(GADError)
    async def h(request, exc):
        from fastapi.responses import JSONResponse
        return JSONResponse(status_code=exc.status_code, content={"detail": exc.detail, "code": exc.code})

    test_sm = async_sessionmaker(db_engine, class_=AsyncSession, expire_on_commit=False)

    async def _session():
        async with test_sm() as s:
            yield s

    from gad.db import get_session
    app.dependency_overrides[get_session] = _session
    app.include_router(auth_router)
    app.include_router(admin_router)
    return app


@pytest.fixture
async def client(app):
    return AsyncClient(transport=ASGITransport(app=app), base_url="http://test")


@pytest.mark.asyncio
async def test_admin_endpoint_returns_403_for_non_admin(client, db_session):
    tokens = await register(
        db_session, RegisterIn(email="u@example.com", password="12345678", display_name="U")
    )
    headers = {"Authorization": f"Bearer {tokens.access_token}"}
    async with client as c:
        resp = await c.get("/admin/stats", headers=headers)
    assert resp.status_code == 403
```

Run: `cd backend && uv run pytest tests/test_admin_moderation.py::test_admin_endpoint_returns_403_for_non_admin -v`

```bash
git add backend/src/gad/exceptions.py backend/src/gad/admin/dependencies.py backend/tests/test_admin_moderation.py
git commit -m "feat(admin): require_admin devuelve 403 Forbidden"
```

---

## Task 2: Schemas de admin

**Files:**
- Modify: `backend/src/gad/admin/schemas.py`

- [ ] **Step 1: Añadir schemas**

En `backend/src/gad/admin/schemas.py`, añadir:

```python
from datetime import date, datetime
from uuid import UUID

from pydantic import BaseModel

from gad.models.enums import UserStatus


class AdminUserOut(BaseModel):
    id: UUID
    email: str
    display_name: str
    status: UserStatus
    is_admin: bool
    reputation_score: float
    created_at: datetime


class UserStatusUpdate(BaseModel):
    status: UserStatus
```

Añadir import de `AdminStatsOut` existente arriba.

- [ ] **Step 2: Commit**

```bash
git add backend/src/gad/admin/schemas.py
git commit -m "feat(admin): schemas para gestión de usuarios"
```

---

## Task 3: set_user_status + ban/suspend/activate

**Files:**
- Modify: `backend/src/gad/users/service.py`
- Modify: `backend/src/gad/admin/service.py`
- Modify: `backend/src/gad/admin/router.py`

- [ ] **Step 1: Service de usuario**

En `backend/src/gad/users/service.py`, añadir:

```python
async def set_user_status(session: AsyncSession, user_id: UUID, status) -> User:
    from gad.models.enums import UserStatus
    result = await session.execute(select(User).where(User.id == user_id))
    user = result.scalar_one_or_none()
    if user is None:
        raise NotFoundError("Usuario no encontrado")
    user.status = UserStatus(status)
    await session.commit()
    await session.refresh(user)
    return user
```

- [ ] **Step 2: Service admin (wraps + revoca tokens)**

En `backend/src/gad/admin/service.py`, añadir:

```python
from gad.users.service import set_user_status


async def ban_user(session: AsyncSession, store, user_id: UUID) -> User:
    user = await set_user_status(session, user_id, "suspended")
    await store.revoke_user(str(user_id), ttl_seconds=7 * 86400)
    return user


async def list_users(
    session: AsyncSession,
    *,
    status: str | None = None,
    limit: int = 50,
    before=None,
) -> list[User]:
    from datetime import datetime as _dt
    stmt = select(User).order_by(User.created_at.desc()).limit(limit)
    if status is not None:
        from gad.models.enums import UserStatus
        stmt = stmt.where(User.status == UserStatus(status))
    if before is not None:
        stmt = stmt.where(User.created_at < before)
    result = await session.execute(stmt)
    return list(result.scalars().all())
```

- [ ] **Step 3: Endpoints**

En `backend/src/gad/admin/router.py`, añadir:

```python
from datetime import datetime
from fastapi import Query
from gad.admin.schemas import AdminUserOut, UserStatusUpdate
from gad.schemas.pagination import PaginatedOut


@router.get("/users", response_model=PaginatedOut[AdminUserOut])
async def list_users_endpoint(
    admin: Annotated[User, Depends(require_admin)],
    session: Annotated[AsyncSession, Depends(get_session)],
    status: str | None = None,
    limit: int = Query(default=50, ge=1, le=100),
    before: datetime | None = Query(default=None),
) -> PaginatedOut[AdminUserOut]:
    users = await list_users_admin(session, status=status, limit=limit, before=before)
    items = [
        AdminUserOut(
            id=u.id, email=u.email, display_name=u.display_name, status=u.status,
            is_admin=u.is_admin, reputation_score=u.reputation_score, created_at=u.created_at,
        )
        for u in users
    ]
    next_cursor = items[-1].created_at.isoformat() if len(items) == limit and items else None
    return PaginatedOut[AdminUserOut](items=items, next_cursor=next_cursor)


@router.post("/users/{user_id}/ban", response_model=AdminUserOut)
async def ban_user_endpoint(
    user_id: UUID,
    admin: Annotated[User, Depends(require_admin)],
    session: Annotated[AsyncSession, Depends(get_session)],
) -> AdminUserOut:
    from gad.auth.dependencies import get_token_store
    user = await ban_user(session, get_token_store(), user_id)
    return AdminUserOut(
        id=user.id, email=user.email, display_name=user.display_name, status=user.status,
        is_admin=user.is_admin, reputation_score=user.reputation_score, created_at=user.created_at,
    )


@router.post("/users/{user_id}/suspend", response_model=AdminUserOut)
async def suspend_user_endpoint(
    user_id: UUID,
    admin: Annotated[User, Depends(require_admin)],
    session: Annotated[AsyncSession, Depends(get_session)],
) -> AdminUserOut:
    return await ban_user_endpoint(user_id, admin, session)


@router.post("/users/{user_id}/activate", response_model=AdminUserOut)
async def activate_user_endpoint(
    user_id: UUID,
    admin: Annotated[User, Depends(require_admin)],
    session: Annotated[AsyncSession, Depends(get_session)],
) -> AdminUserOut:
    from gad.users.service import set_user_status
    user = await set_user_status(session, user_id, "active")
    return AdminUserOut(
        id=user.id, email=user.email, display_name=user.display_name, status=user.status,
        is_admin=user.is_admin, reputation_score=user.reputation_score, created_at=user.created_at,
    )
```

Renombrar el `list_users` del service a `list_users_admin` para coincidir (o ajustar el import).

- [ ] **Step 4: Test + commit**

Añadir a `tests/test_admin_moderation.py`:

```python
@pytest.mark.asyncio
async def test_admin_bans_user_and_revokes_token(client, db_session, redis_client):
    from gad.auth.token_store import TokenStore
    from gad.models.user import User
    from sqlalchemy import update
    import gad.auth.dependencies as deps
    deps._token_store = TokenStore(redis_client)

    admin_tokens = await register(
        db_session, RegisterIn(email="admin@example.com", password="12345678", display_name="A")
    )
    await db_session.execute(
        update(User).where(User.id == admin_tokens.user_id).values(is_admin=True)
    )
    await db_session.commit()

    victim = await register(
        db_session, RegisterIn(email="v@example.com", password="12345678", display_name="V")
    )
    admin_headers = {"Authorization": f"Bearer {admin_tokens.access_token}"}
    victim_headers = {"Authorization": f"Bearer {victim.access_token}"}
    async with client as c:
        # Victim puede acceder
        assert (await c.get("/auth/me", headers=victim_headers)).status_code == 200
        # Admin banea
        resp = await c.post(f"/admin/users/{victim.user_id}/ban", headers=admin_headers)
        assert resp.status_code == 200
        assert resp.json()["status"] == "suspended"
        # Victim ya no puede (token revocado / status no activo)
        assert (await c.get("/auth/me", headers=victim_headers)).status_code == 401
```

Run: `cd backend && uv run pytest tests/test_admin_moderation.py -v`

```bash
git add backend/src/gad/users/service.py backend/src/gad/admin/service.py backend/src/gad/admin/router.py backend/tests/test_admin_moderation.py
git commit -m "feat(admin): ban/suspender/activar usuarios con revocación de tokens"
```

---

## Task 4: Cancelación forzada de plan

**Files:**
- Modify: `backend/src/gad/admin/service.py`
- Modify: `backend/src/gad/admin/router.py`

- [ ] **Step 1: Service**

En `backend/src/gad/admin/service.py`, añadir:

```python
async def force_cancel_plan(session: AsyncSession, plan_id: UUID) -> Plan:
    from gad.models.enums import PlanStatus
    result = await session.execute(select(Plan).where(Plan.id == plan_id))
    plan = result.scalar_one_or_none()
    if plan is None:
        raise NotFoundError("Plan no encontrado")
    plan.status = PlanStatus.cancelled
    await session.commit()
    await session.refresh(plan)
    return plan
```

- [ ] **Step 2: Endpoint**

En `backend/src/gad/admin/router.py`:

```python
@router.post("/plans/{plan_id}/cancel")
async def force_cancel_plan_endpoint(
    plan_id: UUID,
    admin: Annotated[User, Depends(require_admin)],
    session: Annotated[AsyncSession, Depends(get_session)],
) -> dict[str, str]:
    await force_cancel_plan(session, plan_id)
    return {"message": "Plan cancelado por moderación"}
```

- [ ] **Step 3: Test + commit**

```python
@pytest.mark.asyncio
async def test_admin_force_cancel_plan(client, db_session):
    from datetime import UTC, datetime, timedelta
    from gad.models.enums import ActivityType, PlanMode
    from gad.models.plan import Plan
    from gad.models.user import User
    from sqlalchemy import update

    admin = await register(
        db_session, RegisterIn(email="a2@example.com", password="12345678", display_name="A2")
    )
    await db_session.execute(update(User).where(User.id == admin.user_id).values(is_admin=True))
    await db_session.commit()
    plan = Plan(
        host_id=admin.user_id, activity_type=ActivityType.coffee, mode=PlanMode.now,
        title="T", location_label="X", location_grid="SRID=4326;POINT(-58.4 -34.6)",
        expires_at=datetime.now(UTC) + timedelta(hours=2),
    )
    db_session.add(plan)
    await db_session.commit()
    await db_session.refresh(plan)
    headers = {"Authorization": f"Bearer {admin.access_token}"}
    async with client as c:
        resp = await c.post(f"/admin/plans/{plan.id}/cancel", headers=headers)
    assert resp.status_code == 200
```

Run + commit.

```bash
git add backend/src/gad/admin/service.py backend/src/gad/admin/router.py backend/tests/test_admin_moderation.py
git commit -m "feat(admin): cancelación forzada de planes"
```

---

## Task 5: Moderación de reviews

**Files:**
- Modify: `backend/src/gad/admin/service.py`
- Modify: `backend/src/gad/admin/router.py`

- [ ] **Step 1: Service**

En `backend/src/gad/admin/service.py`, añadir:

```python
async def list_flagged_reviews(session: AsyncSession, *, limit: int = 50, before=None) -> list:
    from gad.models.review import Review
    stmt = (
        select(Review)
        .where(Review.flag.is_not(None))
        .order_by(Review.created_at.desc())
        .limit(limit)
    )
    if before is not None:
        stmt = stmt.where(Review.created_at < before)
    result = await session.execute(stmt)
    return list(result.scalars().all())


async def delete_review_admin(session: AsyncSession, review_id: UUID) -> None:
    from gad.models.review import Review
    result = await session.execute(select(Review).where(Review.id == review_id))
    review = result.scalar_one_or_none()
    if review is None:
        raise NotFoundError("Reseña no encontrada")
    reviewee_id = review.reviewee_id
    await session.delete(review)
    await session.commit()
    from gad.reviews.service import recalc_reputation
    await recalc_reputation(session, reviewee_id)
```

- [ ] **Step 2: Endpoints**

En `backend/src/gad/admin/router.py`:

```python
@router.get("/reviews")
async def list_flagged_reviews_endpoint(
    admin: Annotated[User, Depends(require_admin)],
    session: Annotated[AsyncSession, Depends(get_session)],
    limit: int = Query(default=50, ge=1, le=100),
    before: datetime | None = Query(default=None),
) -> PaginatedOut[dict]:
    reviews = await list_flagged_reviews(session, limit=limit, before=before)
    items = [
        {"id": str(r.id), "match_id": str(r.match_id), "reviewer_id": str(r.reviewer_id),
         "reviewee_id": str(r.reviewee_id), "rating": r.rating, "comment": r.comment,
         "flag": r.flag.value if r.flag else None, "created_at": r.created_at.isoformat()}
        for r in reviews
    ]
    next_cursor = items[-1]["created_at"] if len(items) == limit and items else None
    return PaginatedOut[dict](items=items, next_cursor=next_cursor)


@router.delete("/reviews/{review_id}")
async def delete_review_admin_endpoint(
    review_id: UUID,
    admin: Annotated[User, Depends(require_admin)],
    session: Annotated[AsyncSession, Depends(get_session)],
) -> dict[str, str]:
    await delete_review_admin(session, review_id)
    return {"message": "Reseña eliminada por moderación"}
```

- [ ] **Step 3: Smoke + commit**

Run: `cd backend && uv run pytest tests/test_admin_moderation.py tests/test_smoke_phase5.py -v`

```bash
git add backend/src/gad/admin/service.py backend/src/gad/admin/router.py
git commit -m "feat(admin): moderación de reviews flagueadas"
```

---

## Self-Review (Plan 5)

**Cobertura:**
- ✅ 403 en require_admin → Task 1
- ✅ Schemas → Task 2
- ✅ Ban/suspend/activate + list users → Task 3
- ✅ Cancelar plan forzado → Task 4
- ✅ Moderar reviews → Task 5

**Dependencias:** requiere `UserStatus` de Plan 1. Si Plan 1 no se ejecutó, las referencias a `user.status` fallarán — está documentado en el prerequisito.

**Placeholder scan:** Task 3 Step 3 nota el rename `list_users` → `list_users_admin`; es explícito.
